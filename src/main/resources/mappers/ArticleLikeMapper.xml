<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.ArticleLikeMapper">

    <insert id="insertOne" parameterType="ArticleLike">
        insert into articleLike(memberId, articleNo) values(#{memberId}, #{articleNo});
    </insert>

    <select id="countByArticleLike" parameterType="ArticleLike" resultType="int">
        select count(*) from articleLike where memberId=#{memberId} and articleNo=#{articleNo}
    </select>
<!--    <select id="countByMap" parameterType="Map" resultType="int">-->
<!--        select count(*) from articleLike where memberId=#{memberId} and articleNo=#{articleNo}-->
<!--    </select>-->
<!--    <select id="selectByArticleLike" parameterType="ArticleLike" resultType="ArticleLike">-->
<!--        select * from articleLike where memberId=#{memberId} and articleNo=#{articleNo}-->
<!--    </select>-->


    <delete id="deleteByArticleLike" parameterType="ArticleLike">
        delete from articleLike where memberId=#{memberId} and articleNo=#{articleNo}
    </delete>

<!--    <insert id="insertOne" parameterType="com.example.app.vo.Article">-->
<!--        insert into article(writerId, topic, title, content) values(#{writerId}, #{topic}, #{title}, #{content});-->
<!--    </insert>-->




<!--    <select id="selectAll" resultType="Article">-->
<!--        select * from article order by wroteAt desc-->
<!--    </select>-->
<!--    <select id="selectByTopic" resultType="Article" parameterType="string">-->
<!--        select * from article where topic=#{topic}-->
<!--    </select>-->
<!--    <select id="selectByNo" resultType="Article" parameterType="int">-->
<!--        select * from article where no=#{no}-->
<!--    </select>-->



<!--    <update id="increaseViewCnt" parameterType="int">-->
<!--        update article set viewCnt=viewCnt+1 where no=${no};-->
<!--    </update>-->
<!--    <update id="increaseLikeCnt" parameterType="int">-->
<!--        update article set likeCnt=likeCnt+1 where no=${no};-->
<!--    </update>-->


</mapper>